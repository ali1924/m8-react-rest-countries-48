import React from 'react';
import './Country.css';
const Country = (props) => {
    const country = props.country;
    console.log(country);
    return (
        <div>
            
        </div>
    );
};

export default Country;